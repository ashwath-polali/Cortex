## brain files
create your markdown files here — they are gitignored and stay on your machine.
run app.py once to auto-create starter files with placeholder content.
edit them to reflect your own data. they will never be pushed to git.
